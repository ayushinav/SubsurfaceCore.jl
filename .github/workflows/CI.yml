name: CI
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    tags: '*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref_name != github.event.repository.default_branch || github.ref != 'refs/tags/v*' }}

jobs:
  tests:
    runs-on: 'ubuntu-latest'
    name: "${{ matrix.group }} (Julia ${{ matrix.version }})"
    strategy:
      fail-fast: false
      matrix:
        version: ["1.11"]
        group:
          - "transformation"
          - "utils"
          - "turing"
          - "pigeons"
    steps:
      - uses: actions/checkout@v6

      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}

      - uses: julia-actions/cache@v3

      - uses: julia-actions/julia-buildpkg@v1
        if: ${{ matrix.group != 'turing' && matrix.group != 'pigeons' }}

      - name: "Run tests"
        if: ${{ matrix.group != 'turing' && matrix.group != 'pigeons' }}
        uses: julia-actions/julia-runtest@v1
        with:
          coverage: true
        env:
          GROUP: "${{ matrix.group }}"

      - name: "Run extension tests : Turing"
        if: ${{ matrix.group == 'turing' }}
        run: |
          julia --project=test/turing --color=yes -e 'using Pkg; Pkg.instantiate()'
          julia --project=test/turing --color=yes --code-coverage=user test/turing/runtests.jl

      - name: "Run extension tests : Pigeons"
        if: ${{ matrix.group == 'pigeons' }}
        run: |
          julia --project=test/pigeons --color=yes -e 'using Pkg; Pkg.instantiate()'
          julia --project=test/pigeons --color=yes --code-coverage=user test/pigeons/runtests.jl

      - uses: julia-actions/julia-processcoverage@v1
        with:
          directories: "src"

      - uses: codecov/codecov-action@v5
        with:
          files: lcov.info
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
